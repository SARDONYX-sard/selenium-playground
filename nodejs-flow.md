```plantuml
title:Node.jsのアクティビティ図

start
note right:「Next」ボタン押下
' ---------------------------------------------------
:この場で問われている質問：規約に同意するか？\n「I accept the terms in the license agreement」という項目をクリックしてチェックマークを付与。;

      ->[「Next」ボタン押下];

    :この場で問われている質問：インストール先を変えるか？;
      ->[何も変えずに「Next」ボタン押下];

    :この場で問われている質問：インストールするものを選ぶか？;
      ->[何も変えずに「Next」ボタン押下];

    :この場で問われている質問： \n「C」や「C++」で書かれたモジュールのコンパイラツールを自動インストールするか？ \n \n 誰かが作った一部のプログラムコードは、「C」や「C++」と呼ばれる言語を\nコンピュータ上で実行可能なものへ変える必要がある。後々便利なのでチェックを付与。;

      ->[「Next」ボタン押下];

    :「Install」と書かれたボタンを押下;
        stop
    note right: 終了
' ---------------------------------------------------
```
